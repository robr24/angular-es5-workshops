(function() {
    'use strict';

    angular
        .module('scApp')
        .controller('LoginController', Controller);

    // Controller.$inject = ['dependencies'];

    /* @ngInject */
    function Controller() {
        var vm = this;

        activate();

        ////////////////

        function activate() {
        }
    }
})();